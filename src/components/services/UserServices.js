// função para fazer o get do perfil do user, same as in Home.js

//  const { isLoading, error, data } = useFetch(
// "http://localhost:8080/scrumpurrfect/rest/user/profile", {
// method: "GET",
// headers: {
// "Content-Type": "application/json",
// Accept: "*/*",
// token: token,
// selectedUser: selectedUser,
// },
// }
// );
// if (isLoading) return Swal.showLoading();
// if (error) return <div>Error: {error.message}</div>;
// return (

// <div>
// {data.map((user) => (
// <div key={user.username}>
// <h1>{user.username}</h1>
// <p>{user.email}</p>
// <p>{user.firstname}</p>
